apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: attenza-ingress
  namespace: attenza
spec:
  rules:
    - http:
        paths:
          # Backend APIs
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080

          # AI Endpoints (IMPORTANT FIX)
          - path: /ai
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080

          # Frontend
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80